import { useEffect, useState } from 'react';
import { CalendarDays, CheckCircle2, CreditCard, ReceiptText } from 'lucide-react';
import { api, money } from '../api/client';
import { PublicHeader } from '../components/PublicHeader';

type Dashboard = { subscription:any; paymentMethod:any; invoices:any[] };
export function CustomerDashboard(){const [data,setData]=useState<Dashboard|null>(null);const [error,setError]=useState('');useEffect(()=>{api<Dashboard>('/me/dashboard').then(setData).catch(e=>setError(e.message));},[]);return <><PublicHeader/><main className="dashboard-page"><div className="dashboard-heading"><h1>Área do cliente</h1><p>Acompanhe sua assinatura e cobranças do XNaMai Club.</p></div>{error&&<div className="error-box">{error} — escolha um plano e conclua a assinatura para popular esta tela.</div>}{data&&<><section className="customer-top"><div className="panel current-plan"><div><small>PLANO ATUAL</small><h2>{data.subscription?.plan?.name||'Sem plano'}</h2><div className="big-number">{money(data.subscription?.plan?.monthlyPriceCents)}<small>/mês</small></div></div><div className="status-line"><CheckCircle2/><span>Status</span><b>{data.subscription?.status}</b></div><div className="status-line"><CreditCard/><span>Pagamento</span><b>{data.paymentMethod?.type==='CREDIT_CARD'?`${data.paymentMethod.cardBrand||'Cartão'} •••• ${data.paymentMethod.cardLastFour||'----'}`:'PIX recorrente'}</b></div></div><div className="metric-card"><CheckCircle2/><span>Status da assinatura</span><strong>{data.subscription?.status||'—'}</strong></div><div className="metric-card"><CalendarDays/><span>Próxima cobrança</span><strong>{data.subscription?.currentPeriodEnd?new Date(data.subscription.currentPeriodEnd).toLocaleDateString('pt-BR'):'—'}</strong></div></section><section className="panel table-panel"><div className="section-title"><ReceiptText/><h2>Últimas cobranças</h2></div><table><thead><tr><th>Data</th><th>Descrição</th><th>Status</th><th>Valor</th></tr></thead><tbody>{data.invoices.map(inv=><tr key={inv.id}><td>{new Date(inv.createdAt).toLocaleDateString('pt-BR')}</td><td>Mensalidade {inv.subscription?.plan?.name}</td><td><span className={`badge ${inv.status==='PAID'?'success':'danger'}`}>{inv.status}</span></td><td>{money(inv.amountCents)}</td></tr>)}</tbody></table></section></>}</main></>}
